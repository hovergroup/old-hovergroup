//---------------------------------------------------------
// pMOOSBridge_%(VNAME) Config Block  (Created from Plugin)

ProcessConfig = pShare
{
  AppTick   = 10
  MaxAppTick = 20
  IterateMode = 1
  
  output = src_name=NODE_REPORT_LOCAL, dest_name=NODE_REPORT, route=$(SHOREHOST):$(SLPORT)
  
  input = route=$(VHOST):$(LPORT
  input = route=multicast_1
}
