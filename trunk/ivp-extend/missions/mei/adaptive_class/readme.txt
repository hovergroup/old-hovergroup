set MISSION_MODE = HEADING

use CONST_HEADING_UPDATES and poke "heading=x"
use CONST_SPEED_UPDATES and poke "speed=x"

default speed factor is 30, so 2 speed = 60% thrust

post true/false to UTS_PAUSE to turn on utimerscript's thrust output